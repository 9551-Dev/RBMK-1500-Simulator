# Building with Maven

To build with maven, make sure you are running Java8 JDK and run this build command in the root of the project

```bash
mvn clean package
```

you should then get a `RBMK-1500-Simulator-X.X.X-jar-with-dependencies.jar` in the `target` directory
