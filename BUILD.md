# Building with Maven

To build with maven, make sure you are running Java21 JDK and run this build command in the root of the project

```bash
mvn clean package
```

you should then get a `RBMK-1500-Simulator-X.X.X.jar` in the `target` directory
